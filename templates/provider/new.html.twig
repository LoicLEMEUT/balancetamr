{% extends 'base.html.twig' %}

{% block title %}{{ "provider.action.create" | trans | capitalize }}{% endblock %}

{% block body %}
    <!-- Work -->
    <article id="work" class="wrapper style1" style="padding-top: 0.5em">
        <div class="container">
            <header>
                <h2>{{ "provider.action.create" | trans | capitalize }}</h2>
            </header>
            <div class="row aln-center">
                {{ include('provider/_form.html.twig') }}
            </div>
            <footer>
                <a href="{{ path('provider_index') }}" class="button large scrolly">{{ "action.back" | trans | capitalize }}</a>
            </footer>
        </div>
    </article>
{% endblock %}
